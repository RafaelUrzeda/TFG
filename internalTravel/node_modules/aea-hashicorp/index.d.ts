export type Consul = {
    init: (objectData: object, config: Config) => Promise<void>;
    getAllocation: (service: string) => string;
    stopJobDiscovery: () => void;
    stopJobKV: () => void;

}

export type Config = {
    discovery: { [key: string]: string };
    kv: boolean;
}

export const consul: Consul;

export type Vault = {
    init: () => Promise<void>;
}

export const vault: Vault;
