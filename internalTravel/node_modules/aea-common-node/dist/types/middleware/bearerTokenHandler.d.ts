import { FastifyReply, FastifyRequest } from "fastify";
export declare const checkBearerScope: (scope: string, excludes?: string[]) => ((req: FastifyRequest, res: FastifyReply, done: () => void) => void);
