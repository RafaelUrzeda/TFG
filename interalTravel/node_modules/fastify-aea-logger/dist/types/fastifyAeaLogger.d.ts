import { FastifyPluginCallback } from "fastify";
declare const symbolNow: unique symbol;
declare module "http" {
    interface IncomingMessage {
        [symbolNow]: number;
    }
}
interface FastifyOpts {
    exclude?: string | string[];
}
export declare const fastifyAeaLogger: FastifyPluginCallback<FastifyOpts>;
export {};
