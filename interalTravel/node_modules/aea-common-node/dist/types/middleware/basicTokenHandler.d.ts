import { FastifyReply, FastifyRequest } from "fastify";
export declare const checkBasicToken: (basic: string, excludes?: string[]) => ((req: FastifyRequest, res: FastifyReply, done: () => void) => void);
